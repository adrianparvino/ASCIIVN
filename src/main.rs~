mod buffer;
mod renderer;

use buffer::asciibuffer::Asciibuffer;
use buffer::imagebuffer::Imagebuffer;
use renderer::fill::render_fill;

fn main () {
    render_fill(
        Imagebuffer {
            width: 0,
            height: 0,
            background: 0,
            color_type: 0,
            pixel_size: 0,
            buffer: Vec::new()
        }
    );
}
