use buffer::imagebuffer::Imagebuffer;
use buffer::asciibuffer::Asciibuffer;

pub fn render_fill(dst: Imagebuffer) -> Asciibuffer
{
    Asciibuffer {
        width: 0,
        height: 0,
        background: 0,
        color_type: 0,
        pixel_size: 0,
        buffer: ""
    }
}
               
